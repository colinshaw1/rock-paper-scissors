/**
 * Self contains all code within the game to one function
 * Scopes all to the one fucntions
 */
const game = () => {
    let pScore = 0;
    let cScore = 0;

    const startGame = () => {
        const playBtn = document.querySelector(".intro Button");
        const intoScreen = document.querySelector("intro")
    }
}
